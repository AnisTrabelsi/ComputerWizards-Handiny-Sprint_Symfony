<!DOCTYPE html>

<title>New ReservationCovoiturage</title>

{% block body %}
    <h1>Create new ReservationCovoiturage</h1>

    {{ include('reservation/_form.html.twig') }}

    <a href="{{ path('app_reservation_index') }}">back to list</a>
{% endblock %}
