<!DOCTYPE html>

<title>ReservationCovoiturage</title>

{% block body %}
    <h1>ReservationCovoiturage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_covoiturage.id }}</td>
            </tr>
            <tr>
                <th>Prix_covoiturage</th>
                <td>{{ reservation_covoiturage.prixCovoiturage }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ reservation_covoiturage.depart }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ reservation_covoiturage.destination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_index') }}">back to list</a>

    <a href="{{ path('app_reservation_edit', {'id': reservation_covoiturage.id}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
