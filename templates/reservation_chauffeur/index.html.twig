{% extends 'baseB.html.twig' %}

{% block title %}ReservationChauffeur index{% endblock %}

{% block body %}
<br><br><br>


    <h1>ReservationChauffeur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdReservationChauffeur</th>
                <th>DureeService</th>
                <th>DatePriseEnCharge</th>
                <th>DescriptionDemande</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation_chauffeur in reservation_chauffeurs %}
            <tr>
                <td>{{ reservation_chauffeur.idReservationChauffeur }}</td>
                <td>{{ reservation_chauffeur.dureeService }}</td>
                <td>{{ reservation_chauffeur.datePriseEnCharge ? reservation_chauffeur.datePriseEnCharge|date('Y-m-d') : '' }}</td>
                <td>{{ reservation_chauffeur.descriptionDemande }}</td>
                <td>
                    <a href="{{ path('app_reservation_chauffeur_show', {'idReservationChauffeur': reservation_chauffeur.idReservationChauffeur}) }}">show</a>
                    <a href="{{ path('app_reservation_chauffeur_edit', {'idReservationChauffeur': reservation_chauffeur.idReservationChauffeur}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservation_chauffeur_new') }}">Create new</a>
{% endblock %}
