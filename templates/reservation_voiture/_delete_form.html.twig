<form method="post" action="{{ path('app_reservation_voiture_delete', {'id_reservation_voiture': reservation_voiture.id_reservation_voiture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_voiture.id_reservation_voiture) }}">
    <button class="btn">Delete</button>
</form>
